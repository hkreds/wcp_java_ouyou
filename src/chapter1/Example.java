package chapter1;

import chapter1.inner_class_sample.Outer;

public class Example {
	public static void main(String[] args) {
		Outer outer = new Outer();
		Outer.Inner inner = new Outer.Inner();
	}
}
